image: 'Visual Studio 2017'

# Build configuration
configuration:
  - Release

install:
  - C:\msys64\usr\bin\bash -lc "pacman -S --noconfirm make"

build_script:
  - C:\msys64\usr\bin\bash -lc "cd %APPVEYOR_BUILD_FOLDER && pwd && ls && make"


# Artifacts
#artifacts:
#  - path: "cutter-deps-windows.zip"
#
#deploy:
#  description: 'cutter-deps'
#  provider: GitHub
#  auth_token:
#    secure: 
#  artifact: "%ARTIFACT_NAME%"
#  draft: false
#  prerelease: true
#  on:
#    appveyor_repo_tag: true
#
